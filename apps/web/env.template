# Convex Configuration
# CONVEX_URL is automatically set by the Vercel Convex integration
# You only need to set CONVEX_DEPLOY_KEY
CONVEX_DEPLOY_KEY="your-convex-deploy-key-here"
# CONVEX_URL="https://your-deployment.convex.cloud"  # Auto-set by integration

# Self-Update System Configuration (Optional)
# These variables configure the upstream repository for update checking.
# If not set, defaults to "vignesh/voka"
# NEXT_PUBLIC_UPSTREAM_OWNER="vignesh"
# NEXT_PUBLIC_UPSTREAM_REPO="voka"

# Note: The following variables are automatically provided by Vercel when
# "Automatically expose System Environment Variables" is enabled in Project Settings:
# - NEXT_PUBLIC_VERCEL_GIT_REPO_OWNER (your GitHub username)
# - NEXT_PUBLIC_VERCEL_GIT_REPO_SLUG (your repository name)
# These are required for the "Update Now" button to work correctly.
